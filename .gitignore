node_modules
.env
.env.local
.next
*.log

# clerk configuration (can include secrets)
/.clerk/
.claude-pat

# Personal office docs / spreadsheets — keep them out of the repo so a
# blanket push can never sweep them in (see NGFsystems-PL-Workbook incident).
NGFsystems-PL-Workbook-2026.xlsx
/*.xlsx
/*.docx

# Session scratch docs — temporary handoff/plan files kept on disk only.
# Ignored so a blanket `git add` can never commit them to main.
/PORTAL-AUDIT-HANDOFF.md
/IMAGE-PIPELINE-HARDENING.md
